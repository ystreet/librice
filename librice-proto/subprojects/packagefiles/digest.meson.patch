--- /dev/null	2024-02-14 20:31:11.878000303 +1100
+++ digest-0.10.7/meson.build	2024-02-14 20:31:51.693790551 +1100
@@ -0,0 +1,33 @@
+project('digest-rs', 'rust',
+  version : '0.10.7',
+  meson_version : '>= 1.3.1',
+  default_options : ['buildtype=debugoptimized',
+                     'rust_std=2018'
+                    ]
+)
+
+rustc = meson.get_compiler('rust')
+
+crypto_common_dep = dependency(
+  'crypto-common-rs',
+  version : ['>= 0.1.3', '< 0.2']
+)
+subtle_dep = dependency(
+  'subtle-rs',
+  version : ['>= 2.4', '< 3']
+)
+block_buffer_dep = dependency(
+  'block-buffer-rs',
+  version : ['>= 0.10', '< 0.11']
+)
+lib = static_library(
+  'digest',
+  'src/lib.rs',
+  rust_args: ['--cfg', 'feature="core-api"', '--cfg', 'feature="mac"'],
+  dependencies: [block_buffer_dep, crypto_common_dep, subtle_dep],
+  rust_dependency_map : {}
+)
+
+dep = declare_dependency(link_with : lib)
+meson.override_dependency('digest-rs', dep)
+
