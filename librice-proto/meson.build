project('librice-proto', 'rust',
  version : '0.0.1',
  meson_version : '>= 1.3',
  default_options : ['buildtype=debugoptimized',
  'rust_std=2021'])

librice_version = '>= @0@'.format(meson.project_version())

subdir('src')
